/**
 * 间隔组件配置（统一导出）
 * 从各模块重导出所有 spacer 配置
 */

// 从 header 模块导出
export { headerLeftSpacer, headerRightSpacer, headerSpacer } from './header';

// 从 pinRow 模块导出
export { pinRowLeftSpacer, pinRowRightSpacer, pinRowSpacer } from './pinRow';

// 从 attrArea 模块导出
export { attrLeftSpacer, attrRightSpacer } from './attrArea';

// 从 summary 模块导出
export { summaryLeftSpacer, summaryRightSpacer } from './summary';
